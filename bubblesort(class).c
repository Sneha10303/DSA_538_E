#include<stdio.h>
struct node
{
    int data;
    struct node *next;
};
struct node *bubblesort(struct node *head);
{
    int *temp,*temp1,*newnode;
    if(head==NULL)
    {
        printf("there are no numbers to sort\n");
    }
    else
    {
        temp=head;
        while(temp!=NULL)
        {

        }
    }
}
struct node *bubblesort(struct node *head)
{
    int *n,*temp,*temp1;
    for(i=0;i<n-1;i++)
    {
        for(j=0;i<n-1-j;j++)
        {

        }
    }
};
